import React from 'react'
import HomePage from './HomePage'

function App() {
    return (
        <div>
            <HomePage />
        </div>
    )
}

export default App

// npm run build
// npm i -g serve
// serve -s build

//git config --global user.email "username@example.com"
// git status
// git add .
// git commit -m"poruka"

// GitHub
// Create new repository
// Bez Initialize README jer vec ima u reactapp
// git remote add origin https://github.com/pionirtito/myapp.git // -> gde da pushuje
// git push -u origin master